name: cyber_titan_ai
description: "AI-powered Wi-Fi defense system with Guardian AI"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 🌐 WebView (already used)
  webview_flutter: ^4.7.0

  # 🔊 Text to Speech
  flutter_tts: ^3.8.5

  # 🎤 Speech to Text
  speech_to_text: ^6.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/robot/
